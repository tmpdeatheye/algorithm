#include <bits/stdc++.h>

using namespace std;

int t, a, b;

void solve() {
    cin >> a >> b;

}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cin >> t;
    // t = 1;
    while (t--) {
        solve();
    }
    return 0;
}
